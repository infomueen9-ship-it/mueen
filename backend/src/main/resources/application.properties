# Database - Neon
spring.datasource.url=jdbc:postgresql://ep-ancient-voice-aqvgoyp6.c-8.us-east-1.aws.neon.tech/neondb?sslmode=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_GLKbpJ73azAg
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.open-in-view=false

# Flyway
spring.flyway.locations=classpath:db/migration/public
spring.flyway.schemas=public
spring.flyway.baseline-on-migrate=true

# Server
server.port=${PORT:8080}

# JWT
app.jwt.secret=mueen_jwt_secret_key_2024_very_long_string_min_256_bits
app.jwt.expiration=86400000

# Disable Redis
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.data.redis.RedisAutoConfiguration,org.springframework.boot.autoconfigure.data.redis.RedisRepositoriesAutoConfiguration

# Logging
logging.level.com.mueen=INFO